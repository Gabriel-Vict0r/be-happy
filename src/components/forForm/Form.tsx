import React from "react";
import Input from "./Input";

type Props = {};

const Form = (props: Props) => {
  return (
    <form>
      <div>
      </div>
        <Input />
    </form>
  );
};

export default Form;
